import React from 'react';
import { Paginator } from 'react-boostrap'
import { LinkContainer } from 'react-router-bootstrap'

const Paginate = ({pages, page, keyword="", isAdmin=false}) =>{

    return(
        <div>
        </div>
    )
}
export default Paginate 